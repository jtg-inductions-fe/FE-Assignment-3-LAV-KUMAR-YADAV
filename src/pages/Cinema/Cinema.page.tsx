import { Cinema as CinemaContainer } from '@/containers';

/**
 * A page which shows all the movies with their slots in a particular cinema with cinema location
 */
export const Cinema = () => <CinemaContainer />;
